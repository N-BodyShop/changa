module CacheManager {
	readonly CProxy_CacheManager cacheManagerProxy;

	group CacheManager {
		entry CacheManager();
		entry void recvNodes(Key key,int from,CacheNode node_array);
		entry void cacheSync();
		entry void recvParticles(Key key,GravityParticle part[num],int num,int from);
	};
	
};
