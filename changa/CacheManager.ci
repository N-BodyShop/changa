module CacheManager {
	readonly CProxy_CacheManager cacheManagerProxy;

	group CacheManager {
		entry CacheManager();
		entry void recvNodes(Key key,int from,CacheNode &node);
		entry void recvNodes(int count,Key keys[count],CacheNode nodes[count],int );
		entry void cacheSync(unsigned int );
		entry void recvParticles(Key key,GravityParticle part[num],int num,int from);
	};
	
};
